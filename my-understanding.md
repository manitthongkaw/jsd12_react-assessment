# ขั้นตอนที่ผมทำงานครับ
- ติดตั้ง Vite React JS + Tailwind CSS
- เตรียม Home.jsx , Owner.jsx ไว้ใน /pages
- สร้าง Router ใน App.jsx
- สร้าง React Context แล้วเรียกใน main.jsx
- เช็คความถูกต้อง npm run dev
- พบว่าลืมติดตั้ง npm i react-router-dom
- และพบว่าสร้าง /contexts /pages ไว้ผิดที่ จึงย้ายมาไว้ใน /src
- เช็คความถูกต้องอีกครั้ง
- รันหน้าได้ปกติ และ Router ทำงาน
- - http://localhost:5173/ แสดง Home
- - http://localhost:5173/owner แสดง Owner
- สร้างข้อมูลหลัก และปุ่มในหน้า Home
- ทำฟังก์ชั่นของปุ่มหน้า Home โดยใช้ useState แล้วเรียกใช้ผ่าน useContext
- สร้างข้อมูลในหน้า Owner
- สร้าง Navbar.jsx แล้วนำมาใส่ที่ Home, Owner components
- เนื่องจากมี 2 หน้าเลยไม่ทำ Layout Router
- สร้างตารางแสดงผลของ Home User
- สร้างตารางแสดงผลของ Home Admin
- สร้างแบบฟอร์มสำหรับเพิ่มข้อมูลของ Home Admin

- เริ่มสร้าง fetch ทั้ง 3 แบบ คือ get, post & delete ใน /api/member.js
- เพิ่ม useEffect เพื่อใช้ดึงข้อมูลจาก api เมื่อมีการเปลี่ยนแปลงข้อมูล members ใน MessageProvider.jsx แล้วดึงไปแสดงในตารางของ HomeUser.jsx และ HomeAdmin.jsx ด้วย useContext
- ใช้การ .sort เพื่อเรียงข้อมูลให้แสดง id ล่าสุดก่อน แล้ว .map ลงในตารางข้อมูล
- พบปัญหามี lastName ไม่แสดง เลยไปเช็คมีเป็น key lastname
- หลังจากแสดงผลได้แล้ว ก็ทำในส่วนของ formData มีการเก็บค่าเป็น {object}
- สร้าง handleChange เพื่อเช็ค value ใน input
- สร้าง handleSubmit ไว้ส่งข้อมูลไปที่ handleCreateMember
- การเพิ่มข้อมูลไม่มีปัญหา
- สร้าง handleDeleteMember สำหรับปุ่มลบข้อมูลออก โดยอ้างอิงจาก member.id
- ลองลบข้อมูลสามารถลบได้ แต่ถ้ากดรัว ๆ จะ 404 ใน id เดิมที่เคยกดลบ แต่ข้อมูลยังไม่หายไป

- ในงานทั้งหมด ผมเลือกใช้ผ่าน useContext ทั้งหมด โดยอยากให้ชิน เมื่อต้องนำไปใช้กับโปรเจคหรืองานจริงที่ใหญ่กว่านี้
- ผมเลือกใช้ fetch แบบ async await เพื่อให้การดึงข้อมูลทะยอดส่งกลับมาให้เราเมื่อพร้อม